import React from "react";

/**
 * AdminHomePage.tsx
 *
 * Admin home page at /admin/home.
 * In accordance with Jimmy's specifications, the page body is currently empty.
 * Navigation and avatar menu are provided by the enclosing Admin layout.
 */
export const AdminHomePage: React.FC = () => {
  return (
    <div className="w-full min-h-[calc(100vh-4rem)] bg-brand-bg" />
  );
};

export default AdminHomePage;
